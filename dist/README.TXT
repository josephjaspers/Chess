

Add the images from the Chess folder to whatever folder the Chess.jar file is in (default is dist)

chmod +x Chess.jar   //enables execution
sudo ./Chess.jar     //runs the program

Running the program by double clicking may result in images not appearing. 

